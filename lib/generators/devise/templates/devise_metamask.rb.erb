# frozen_string_literal: true

# This initializer was generated by devise_metamask.  It defines default
# parameter names and attribute names used by the MetaMask authentication
# strategy.  You can customise these values or remove this file entirely if
# you prefer to configure them directly in config/initializers/devise.rb.

Devise.setup do |config|
  # Parameters expected from the MetaMask login form
  config.metamask_address_param  ||= 'metamask_address'
  config.metamask_message_param  ||= 'metamask_message'
  config.metamask_signature_param ||= 'metamask_signature'

  # Attributes on your model storing the Ethereum address and nonce
  config.metamask_eth_attribute   ||= :eth_address
  config.metamask_nonce_attribute ||= :metamask_nonce
end